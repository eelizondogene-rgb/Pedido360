@model IEnumerable<Pedido360.ViewModels.ClienteListItemViewModel>
@{
    ViewData["Title"] = "Clientes";
}

<div class="page-header d-flex justify-content-between align-items-end flex-wrap gap-2">
    <div>
        <div class="page-eyebrow">Pedidos360 / Clientes</div>
        <h1 class="h3 mb-1">Clientes</h1>
        <p class="text-muted mb-0">Directorio de clientes B2B habilitados para hacer pedidos.</p>
    </div>
    <a class="btn btn-primary" asp-action="Create">Nuevo cliente</a>
</div>

@* Barra de busqueda por nombre o cedula *@
<form method="get" class="mb-3">
    <div class="input-group" style="max-width: 400px;">
        <input type="text"
               name="buscar"
               value="@ViewBag.Buscar"
               class="form-control"
               placeholder="Buscar por nombre o cedula..." />
        <button type="submit" class="btn btn-outline-secondary">Buscar</button>
        @if (!string.IsNullOrWhiteSpace(ViewBag.Buscar as string))
        {
            <a asp-action="Index" class="btn btn-outline-danger">Limpiar</a>
        }
    </div>
</form>

<div class="table-card">
    <table class="table table-hover align-middle mb-0">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Cedula</th>
                <th>Correo</th>
                <th>Telefono</th>
                <th>Estado</th>
                <th class="text-end">Acciones</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="num">@item.Cliente.Id</td>
                <td>@item.Cliente.Nombre</td>
                <td class="num">@item.Cliente.Cedula</td>
                <td>@item.Cliente.Correo</td>
                <td class="num">@item.Cliente.Telefono</td>
                <td><partial name="_ActivoTag" model="@item.Cliente.Activo" /></td>
                <td class="text-end">
                    <a class="btn btn-sm btn-outline-primary"
                       asp-action="Details"
                       asp-route-id="@item.Cliente.Id">Ver</a>
                    <a class="btn btn-sm btn-outline-secondary"
                       asp-action="Edit"
                       asp-route-id="@item.Cliente.Id">Editar</a>
                    @if (User.IsInRole("Admin"))
                    {
                        <a class="btn btn-sm btn-outline-danger"
                           asp-action="Delete"
                           asp-route-id="@item.Cliente.Id">Eliminar</a>
                    }
                </td>
            </tr>
        }
        @if (!Model.Any())
        {
            <tr>
                <td colspan="7" class="text-center text-muted py-4">
                    @if (!string.IsNullOrWhiteSpace(ViewBag.Buscar as string))
                    {
                        <span>No se encontraron clientes con ese criterio de busqueda.</span>
                    }
                    else
                    {
                        <span>No hay clientes registrados aun.</span>
                    }
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
