@{
    ViewData["Title"] = "Acceso restringido";
    Layout = "_Layout";
}

<div class="form-card p-5 text-center" style="max-width: 560px; margin: 3rem auto;">
    <div class="page-eyebrow mb-2" style="color: var(--brick);">Error 403</div>
    <h1 class="h3 mb-2">No tenes permiso para ver esto</h1>
    <p class="text-muted">Tu rol actual no tiene acceso a esta seccion. Si crees que es un error, consulta con un administrador.</p>
    <a class="btn btn-primary mt-2" asp-controller="Home" asp-action="Index">Ir al panel</a>
</div>
